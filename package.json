{
  "name": "vitale-containers",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c auto \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "dev:32": "concurrently -n FRONTEND,BACKEND -c auto \"npm run dev:32 --workspace frontend\" \"npm run dev --workspace backend\"",
    "clean:frontend": "npm run clean --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
